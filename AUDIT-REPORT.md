# 🔍 تقرير فحص التطبيق

> فحص حقيقي بتشغيل السيرفر وضرب كل نقطة API بنداءات فعلية —
> مش قراءة كود وتخمين.

**الطريقة:** شغّلت السيرفر الحقيقي (`src/app.js`) ببدائل في الذاكرة
لـ Postgres/Redis/الطوابير، وضربت كل نقطة بيستخدمها التطبيق بنفس
الترتيب اللي المستخدم بيمشي بيه — تسجيل، أونبوردنج، حلم، مهام، عشائر،
رسائل، منبه، تركيز، دعوات.

---

## ✅ النتيجة: 26 من 28 نقطة شغالة

| الشاشة | النقطة | النتيجة |
|---|---|---|
| **الدخول** | `POST /auth/register` | ✅ 201 + توكن حقيقي |
| | `POST /auth/login` | ✅ 200 |
| | `POST /auth/onboarding` | ✅ 200 |
| | `PATCH /auth/companion` | ✅ 200 |
| | `GET /auth/me` | ✅ 200 |
| **الجبل** | `GET /goals` | ✅ 200 |
| | `POST /goals/dream` | ✅ 503 (صح — مفيش مفتاح Gemini) |
| **المهام** | `GET /tasks` | ✅ 200 |
| | `POST /tasks` | ✅ 201 |
| | `PATCH /tasks/:id/complete` | ✅ 200 |
| **العشائر** | `GET /clans/my-clans` | ✅ 200 |
| | `POST /clans/global/auto-assign` | ✅ 200 |
| | `GET /clans/:id/members` | ✅ 200 |
| | `POST /clans/private/create` | ✅ 201 |
| **الرسائل** | `GET /chat/conversations` | ✅ 200 |
| | `GET /chat/clans` | ✅ 200 |
| | `GET /chat/search` | ✅ 200 |
| | `GET /chat/requests` | ✅ 200 |
| **المنبه** | `GET /alarms` | ✅ 200 |
| | `POST /alarms` | ✅ 201 |
| | `GET /alarms/wake-task` | ✅ 200 + مسألة موقّعة |
| | `DELETE /alarms/:id` | ✅ 200 |
| **التركيز** | `POST /focus/start` | ✅ 201 |
| | `POST /focus/challenge` | ✅ 201 (عشيرة خاصة) |
| **الاطمئنان** | `GET /notifications` | ✅ 200 |
| | `POST /notifications/checkin/open` | ✅ 201 |
| | `GET /notifications/:id/thread` | ✅ 200 |
| | `POST /notifications/:id/reply` | ✅ 200 |

---

## 🐛 باجات حقيقية اتكشفت

### ١. زرار التحدي كان بيتعرض للكل

السيرفر بيفرض **قيدين**:
```
التحديات في العشائر الخاصة فقط
أنت لست صاحب هذه العشيرة
```

والواجهة كانت بتعرض الزرار في **كل** عشيرة. يعني عضو في عشيرة عامة
يضغط ويترفض بلا سبب واضح.

**اتصلح:** الزرار بيبان لصاحب العشيرة الخاصة بس، ومكانه رسالة بتشرح
السبب بدل ما يختفي بلا تفسير.

### ٢. شاشة البروفايل فيها أزرار ميتة وبيانات مخترعة

```dart
_actionTile(..., 'المنبهات', '4:00 فجراً', () {}),   ← وقت مخترع + زرار فاضي
_actionTile(..., 'التوثيق الأسبوعي', '', () {}),      ← زرار فاضي
_actionTile(..., 'الإعدادات', '', () {}),             ← زرار فاضي
```

«4:00 فجراً» **مش من أي منبه حقيقي** — نص مكتوب في الكود بيدّعي بيانات.

**اتصلح:** زرار المنبهات بقى بيفتح شاشة المنبهات فعلاً، والاتنين
التانيين اتشالوا (شاشاتهم لسه ماتبنتش).

### ٣. الفاحص كان بيفوّت نمط الزرار الفاضي

`test/ui-quality.test.mjs` كان بيدوّر على `onPressed: () {}` — لكن
البروفايل كان بيمرر الدالة الفاضية **كوسيط**: `_actionTile(..., () {})`.
وسّعت الفحص ليمسك النمطين.

---

## 📱 حالة الشاشات

| الشاشة | متصلة بالسيرفر؟ |
|---|---|
| الجبل · المهام · العشائر · الأعضاء | ✅ مباشرة |
| الرسائل · المحادثة · المنبه · مهمة الصحيان | ✅ مباشرة |
| غرفة التحدي · إعداد الحلم · التركيز | ✅ مباشرة |
| الدخول · الاهتمامات | ✅ عبر `AppState` |
| **البروفايل** | ⚠️ بيقرا من الذاكرة المحلية بس |

**مفيش شاشة وهمية بالكامل.** البروفايل الوحيدة اللي مش بتنادي السيرفر
مباشرةً — بتعرض بيانات المستخدم المحفوظة محلياً. ده مقبول للاسم
والرفيق، بس **السلسلة والشرارات ممكن يبقوا قدام** لو اتغيروا من جهاز
تاني.

---

## 🔌 فجوات معروفة (السيرفر جاهز والواجهة لسه)

| الفيتشر | نقاط السيرفر |
|---|---|
| الإنجازات والألقاب | 7 |
| التحليلات والإحصائيات | 8 |
| الشات الفوري (Socket.io) | جاهز — الواجهة بتستطلع كل 5 ثواني |
| التوثيق الأسبوعي | 3 |

دي **مش أعطال** — فيتشرز لسه ماتوصلتش. عناوينها موجودة في
`ApiEndpoints` عن قصد عشان الفجوة تفضل باينة.

---

## ⚠️ حدود الفحص ده

شغّلت السيرفر ببدائل في الذاكرة، مش Postgres حقيقي. يعني:

- **العلاقات المتداخلة** (include المتشعب) محاكاة تقريبية — بعض الـ500
  اللي ظهرت كانت من البديل مش من الكود، وتحققت من كل واحدة فيهم
  بالرجوع للمصدر.
- **نداءات Gemini الحقيقية** ماتجربتش (مفيش مفتاح) — بس مسار الخطأ
  اتجرب ورجّع 503 صح.
- **الواجهة نفسها ماتكومبايلتش** — مفيش Flutter SDK في بيئة التطوير.

الفحص ده بيغطي **العقد بين التطبيق والسيرفر**. اللي محتاج جهازك:
`flutter analyze` ثم تشغيل فعلي.
