# ⭐ حلقة الاطمئنان — أهم فيتشر في «بال»

> من أول ما المستخدم يفتح التطبيق لأول مرة، لحد ما الرفيق يسأله عن مهمة النهاردة
> ويرد عليه **جوه نفس البوب-أب**.

---

## 1) الرحلة كاملة

```
 أول دخول
    │
    ├─▶ POST /api/auth/onboarding          اختيار الاهتمامات
    │
    ├─▶ POST /api/goals/dream              { title: "عايز أكون CEO" }
    │        └─ الـ AI يرجّع 4 أسئلة كويز (خلفية · مهارات · وقت · عقبات)
    │
    ├─▶ POST /api/goals/dream/:id/answers  { answers: [...] }
    │        └─ الـ AI يبني حقول الجبل (5–7 خطوات، من القاعدة للقمة)
    │            ⇒ كل حقل = هدف (GoalStep)
    │
    ├─▶ POST /api/goals/dream/:id/approve  المستخدم يوافق → الجبل يتثبّت
    │
    ├─▶ POST /api/goals/steps/:stepId/journey          لكل هدف
    │        └─ الـ AI يوزّعه على أيام (3–30 يوم حسب حجمه)
    │
    └─▶ POST /api/goals/steps/:stepId/journey/approve
             └─ التواريخ تتوزّع بتوقيت المستخدم · مهمة اليوم الأول تتولّد فوراً

 مزامنة يومية (منتصف ليل المستخدم المحلي)
    │
    └─▶ جوب JOURNEY_DAILY كل 15 دقيقة
             └─ لما يوصل منتصف ليل المستخدم (User.timezone) → مهام النهاردة تتولّد
                 · Idempotent: مهمة واحدة لكل يوم (unique على journeyDayId)
                 · إشعار «مهامك لليوم جاهزة 🏔️» + بث socket `tasks:generated`

 وقت المهمة
    │
    ├─▶ قبلها بـ 5 دقايق  → نكشة (TASK_NUDGE)
    │
    └─▶ لما وقتها يخلص    → ⭐ سؤال الاطمئنان (TASK_CHECKIN)
             └─ إشعار data.canReply = true → التطبيق يفتح بوب-أب فيه حقل رد
                 + بث socket `notification:new` (لو التطبيق مفتوح يطلع فوراً)

 الرد جوه البوب-أب
    │
    └─▶ POST /api/notifications/:id/reply  { text: "خلصت نصها بس اتشتت" }
             └─ رد الرفيق يرجع في **نفس الاستجابة** ويتعرض في نفس الحقل
                 + بث socket `checkin:reply` لباقي أجهزة المستخدم
```

---

## 2) توقيتات الاطمئنان

| السبب (`reason`) | إمتى | الجدولة |
|---|---|---|
| `SCHEDULE_END` | لما ينتهي وقت المهمة المجدول | عند إنشاء/تعديل أي مهمة ليها وقت |
| `COMPLETED` | بعد 10 دقايق من إنجاز المستخدم للمهمة يدوياً | عند `POST /api/tasks/:id/complete` |

**مهام الجبل** مالهاش ساعة محددة → السؤال بييجي **9 مساءً بتوقيت المستخدم**
(مش 23:59، عشان يلاقيه صاحي · مش UTC، عشان مستخدم طوكيو ما يتسألش الفجر).

⚠️ **إشعار اطمئنان واحد بس لكل مهمة.** لو المستخدم خلّص المهمة قبل معادها،
اللي يوصل الأول بس هو اللي يتنفّذ — التاني يرجع `skipped: 'ALREADY_CHECKED'`.

---

## 3) الجمل مش ثابتة (ضد الملل)

المشكلة: نفس الصياغة كل مرة = المستخدم يقفل الإشعار من غير ما يقراه.

**طبقتين حماية** في `src/services/checkinPhrases.service.js`:

| الطبقة | إزاي |
|---|---|
| الـ AI شغال | كل مرة بياخد **توجيه أسلوب مختلف** من 10 توجيهات (نكشة / فضول / احتواء / سؤال محدد …) + `temperature: 1.0` |
| الـ AI واقع | اختيار من **12 صيغة** جاهزة، مع **استبعاد آخر 8 صيغ اتستخدمت** لنفس المستخدم (Redis, 14 يوم) |

عنوان الإشعار كمان بيتغيّر (5 صيغ)، مش «اطمئنان» كل مرة.

---

## 4) عقد الـ API للفرونت إند

### `POST /api/notifications/:id/reply`  🔒 JWT

```jsonc
// Request
{ "text": "خلصت نصها بس اتشتت" }

// 200 — رد الرفيق
{
  "success": true,
  "notificationId": "…",
  "reply": "جامد إنك خلصت نصها 👏 إيه اللي وقف قدامك في الباقي؟",
  "source": "AI",        // AI | SYSTEM | CRISIS_PROTOCOL
  "turnsLeft": 9
}

// 200 — أزمة إنسانية (مفيش نداء AI إطلاقاً)
{ "success": true, "crisis": true, "reply": "…", "source": "CRISIS_PROTOCOL" }
```

| كود | الحالة |
|---|---|
| `404 NOTIFICATION_NOT_FOUND` | الإشعار مش موجود أو مش بتاع المستخدم |
| `403 NOTIFICATION_NOT_REPLYABLE` | `data.canReply !== true` |
| `400 REPLY_REQUIRED` / `REPLY_TOO_LONG` | نص فاضي / أطول من 1000 حرف |
| `429 THREAD_LIMIT_REACHED` | 10 ردود في الخيط الواحد |
| `429` (rate limit) | أكتر من 20 رد في الدقيقة |

⚠️ **`source: "SYSTEM"` معناه الـ AI واقع** — الرد مش من الرفيق.
اعرضه بشكل مختلف (رمادي/بلا أفاتار) عشان ما ندّعيش على الرفيق كلام مقالوش.

### `GET /api/notifications/:id/thread`  🔒 JWT

بيرجّع الإشعار + المحادثة كاملة + `canReply` + `turnsLeft`.
استخدمه لما المستخدم يقفل البوب-أب ويفتحه تاني.

### Socket.io — namespace `/notifications`

```js
const socket = io(`${baseUrl}/notifications`, { auth: { token: accessToken } });

socket.on('notification:pending', ({ notifications }) => {/* اتراكموا وهو قافل */});
socket.on('notification:new',     (notification)      => {/* افتح البوب-أب */});
socket.on('checkin:reply',        ({ notificationId, reply }) => {/* جهاز تاني رد */});
socket.on('tasks:generated',      ({ count, titles }) => {/* مهام النهاردة نزلت */});

socket.emit('notification:seen', { notificationId });
```

افتح البوب-أب لما `notification.data.canReply === true`.

---

## 5) باجات اتصلحت في الشغل ده

| الباج | الأثر |
|---|---|
| `persona.build('TASK_FOLLOWUP')` كان بيرجع وضع COMPANION العام | **قواعد «ممنوع اللوم» ما كانتش بتتطبق إطلاقاً** — `TASK_FOLLOWUP` معرّفة في `MOMENTS` والدالة كانت بتقرا `MODES` بس |
| `gemini.generate` كان بيتجاهل `opts.temperature` | كل النداءات على 0.85 — صياغة الاطمئنان كانت بتطلع متشابهة، وهي بالظبط اللي بتقتل الفيتشر |
| مهام الجبل ما كانش ليها اطمئنان | الجدولة كانت في مسار إنشاء المهمة اليدوي بس — يعني **أغلب مهام المستخدم** كانت بتعدي بلا سؤال |
| الاطمئنان بعد الإنجاز اليدوي بس | لو المستخدم ما ضغطش «تمّ»، عمره ما كان هيتسأل |
| نص الاحتياطي واحد ثابت | `شكلك خلصت «س» — عامل إيه؟` كل مرة بالحرف |

---

## 6) الاختبارات

```bash
npm run test:checkin     # 16 اختبار — بلا قاعدة بيانات ولا Redis ولا Gemini
```

| الملف | بيغطي |
|---|---|
| `test/checkin-loop.test.mjs` (7) | البيرسونا · الحرارة · حساب نهاية المهمة · بنك الصيغ · الخيط · البث |
| `test/checkin-reply.test.mjs` (9) | مسار HTTP كامل: ملكية · صلاحية الرد · تحقق · **أزمة بلا نداء AI** · رد سليم · AI واقع · thread |

---

## 7) لسه ناقص

- [ ] **واجهة البوب-أب في `bal_app`** — الباك جاهز، الودجت لسه (مفيش Flutter SDK في بيئة التطوير الحالية للتحقق)
- [ ] توليد رحلات الأهداف تلقائياً بعد الموافقة على الجبل (دلوقتي زر يدوي لكل مرحلة)
- [ ] Push notification فعلي محتاج `FIREBASE_SERVICE_ACCOUNT` — من غيره الإشعار بيوصل لما التطبيق يفتح بس
- [ ] ⚠️ **تدوير `GEMINI_API_KEY`** — المفتاح القديم متسرّب في تاريخ الـ git
