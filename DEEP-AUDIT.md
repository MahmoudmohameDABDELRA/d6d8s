# 🔬 فحص جذري — تقرير بلا تعديل

> كل بند اتنفّذ على سيرفر شغّال أو اتقرا من الكود بالسطر.
> **مفيش أي تعديل اتعمل** — ده تقرير قرار.

---

# 🔴 ثغرات حرجة (بتكسر وعد للمستخدم)

## ١. المنبه مش هيرن والتطبيق مقفول

**السلسلة مقطوعة من الطرفين:**

| الحلقة | الحالة |
|---|---|
| السيرفر يبعت push | ⚠️ محتاج `FIREBASE_SERVICE_ACCOUNT` |
| من غيره | 🔴 بيسجّل `[FCM MOCK DISPATCH]` ويرجّع **`success: true`** |
| التطبيق يسجّل جهازه | 🔴 مفيش أي نداء لـ `/notifications/device` |
| حزمة إشعارات في `pubspec` | 🔴 مفيش (لا Firebase ولا local_notifications) |

**الأثر:** المنبه شغال 100% **وانت فاتح التطبيق بس**. مقفول = مفيش رنين.

**الأخطر:** السيرفر بيقول `success: true` وهو مبعتش حاجة — فمفيش
إنذار إن الإشعارات مش شغالة.

> **رأيي:** ده مش «كمالي». منبه مبيرنش مش منبه. لو هتأجّل حاجة،
> أجّل الأنميشن مش ده.

---

## ٢. كل المستخدمين بتوقيت القاهرة

```
✅ السيرفر بيقبل timezone في التسجيل  (auth.controller.js:280)
🔴 التطبيق مش بيبعتها إطلاقاً
→ الافتراضي Africa/Cairo للكل
```

**الأثر المؤكد بالتشغيل:**
```
Africa/Cairo:      اليوم المحلي 2026-08-27
Asia/Tokyo:        اليوم المحلي 2026-08-28   ← يوم مختلف
America/New_York:  اليوم المحلي 2026-08-27
```

مستخدم في طوكيو: مهامه بتنزل **الساعة 7 صباحاً بتوقيته** بدل
منتصف الليل، وسؤال الاطمئنان بييجي **3 صباحاً**.

**الإصلاح:** سطر واحد — `DateTime.now().timeZoneName` مع التسجيل.

---

## ٣. الجلسة الجارية بتضيع لو قفلت التطبيق

الواجهة بتعد **محلياً** (`Timer.periodic` كل ثانية) وماتسألش
السيرفر عن الطور.

```
🔴 /focus/active — معرّفة في ApiEndpoints ومش مستخدمة
✅ السيرفر بيرجّع phase: FOCUS | REST مع الدقائق المتبقية
```

**الأثر:** قفلت التطبيق في الدقيقة 20 من 25؟ فتحته لقيت الجلسة
اختفت. والسيرفر لسه فاكرها.

---

## ٤. لعبة الثعبان: صفر واجهة

```
🔴 مفيش أي ملف في bal_app بيذكر games/rooms أو الثعبان
```

السيرفر كامل (كود اللعبة 24 كيلوبايت، غرف، دعوات، حماية العشيرة
اللي قفلناها المرة اللي فاتت) — **والمستخدم مايقدرش يوصلها**.

وضع الراحة كمان مفيهوش أي مدخل للعبة.

---

# 🟠 فجوات كبيرة

## ٥. الشات لسه بعيد عن تيليجرام

السيرفر عنده **كل حاجة** والواجهة مستخدمة **ولا واحدة**:

| الميزة | السيرفر | الواجهة |
|---|---|---|
| رد على رسالة | — | ❌ |
| تعديل رسالة | `PATCH /messages/:id` | ❌ |
| حذف رسالة | `DELETE /messages/:id` | ❌ |
| تفاعل (إيموجي) | `POST /messages/:id/react` | ❌ |
| مؤشر الكتابة | حدث `typing` جاهز | ❌ |
| إيصال قراءة | حدث `message_read` جاهز | ❌ |
| تحميل الأقدم | `?before=` مدعوم | ❌ |
| بلاغ وحظر | 3 نقاط | ❌ |

**كمان:** مفيش ترقيم في **11 شاشة** — المحادثة بتحمّل كل الرسايل
مرة واحدة. مع 500 رسالة الشاشة هتتخنق.

## ٦. المنبه: 3 نقاط شغالة مش مستخدمة

```
⚠️ alarmSnooze   — الغفوة بنداء AI (السيرفر شغال، جرّبته)
⚠️ alarmHistory  — تاريخ الاستيقاظ
⚠️ alarmMissed   — تسجيل الفوّت
```

السلسلة (`wakeStreak`) موجودة في القاعدة وبتتعرض — بس **الفوّت مش
بيتسجّل**، فالسلسلة مش دقيقة.

---

# 🟡 الكماليات (اللي سألت عنها)

## ٧. الحركة: 10 من 12 شاشة ساكنة تماماً

```
AnimatedContainer  7
AnimatedScale      2
AnimatedCounter    2
AnimatedBuilder    1
```

الشاشات الساكنة: المنبهات · مهمة الصحيان · الدخول · المحادثة ·
الأعضاء · العشائر · غرفة التحدي · التركيز · الجبل · البروفايل.

مفيش `Hero` ولا انتقالات مخصصة — كل تنقّل بيبان مقطوع.

## ٨. سلّم الخطوط: 23 حجم مختلف

```
10 · 10.5 · 11 · 12.5 · 13 · 13.5 · 14 · 14.5 · 15 · 15.5 · 16
16.5 · 17.5 · 18.5 · 19.5 · 20.5 · 22 · 23 · 25.5 · 27.5 · 32 · 39 · 60
```

**161 مكان** مكتوب فيه الحجم بالإيد، مقابل **10 بس** بيستخدموا
`Theme.of(context).textTheme`.

> **دي جذر مشكلة الاتساق.** الكسور (13.5 · 17.5 · 20.5) جاية من
> تكبير الـ15% اللي عملناه على أرقام يدوية. لو كانت الأحجام في
> الثيم من الأول، كان التكبير سطر واحد.

## ٩. الكروت: 34 حاوية خام مقابل 16 كارت موحّد

`GlassCard` موجود وحلو — ومستخدم في أقل من نصف الأماكن.

## ١٠. مناطق لمس أصغر من المعيار

```
21 عنصر بارتفاع 23px
 7 عناصر بارتفاع 27.5px
```

المعيار العالمي **48×48** كحد أدنى. دي بتخلي الضغط صعب على
الشاشات الصغيرة.

## ١١. التحميل: دايرة فاضية في 16 مكان

```
CircularProgressIndicator: 16
Shimmer / Skeleton:         0
```

الهيكل الوهمي (skeleton) بيخلي الانتظار يبان أقصر — دي أرخص
تحسينة إحساس في التطبيق كله.

---

# ✅ اللي مؤكد شغال

## الجبل والتوقيت (اتنفّذ فعلياً)
```
✅ الجبل اتبنى: هدف → مرحلة → رحلة → 3 أيام
✅ مهمة الجبل ظهرت في المهام
✅ اليوم اللي معاده لسه مجاش بيتأجّل (deferred)
✅ مهمة واحدة لكل يوم (unique) — مفيش تكرار
✅ التوليد بيستخدم منطقة كل مستخدم لوحده
✅ جوب المزامنة كل 15 دقيقة بمعرّف ثابت
✅ كل مستخدم بيتسأل بتوقيته (القاهرة 18:00Z · طوكيو 12:00Z)
```

**سلسلة الجبل → الـ AI → المهام سليمة منطقياً.** الشرط الوحيد:
`GEMINI_API_KEY`. من غيره بيرفض بـ `503 GEMINI_NOT_CONFIGURED`
صراحةً — **ومفيش خطة وهمية**، وده الصح.

## المنبه في السيرفر — 14/14
```
✅ إنشاء · قائمة بالتوقيت المحلي · اليوم الحالي
✅ المسألة موقّعة والإجابة مش ظاهرة في الـ token
✅ إجابة غلط مرفوضة · صح مقبولة · token معدّل مرفوض
✅ الغفوة بترفض صراحةً لما الـ AI مش متاح (502)
✅ منبه مكرر 409 · وقت غلط 400 · بلا أيام 400
```

## الوضع الليلي
```
✅ 16 لون مزدوج · بيتحفظ في SharedPreferences · بيتطبق على الفور
```

---

# 🤔 حاجات ليا رأي مختلف فيها

## «الشات زي تيليجرام بالظبط»

تيليجرام تطبيق **مراسلة**. «بال» تطبيق **إنجاز** فيه مراسلة.

لو نسخنا تيليجرام حرفياً — ستيكرز، قنوات، بوتات، مجلدات — هيبقى
تطبيق شات ضعيف بدل تطبيق إنجاز قوي، والمستخدم هيقعد فيه بدل ما
يخلص مهامه.

**اقتراحي:** ناخد من تيليجرام **الإحساس** (سرعة، فقاعات مريحة،
رد على رسالة، مؤشر كتابة) ونسيب اللي بيحوّله لتطبيق تواصل.
الرسايل في «بال» غرضها التنسيق مع عشيرتك مش الدردشة المفتوحة.

## «الأنميشنات»

الحركة مش زينة — دي **بتشرح** للمستخدم إيه اللي حصل. عندك 3
لحظات تستاهل حركة أكتر من أي حاجة:

1. **إتمام المهمة** — لحظة الإنجاز، محتاجة احتفال
2. **الصعود في الجبل** — التقدم لازم يتشاف
3. **البوب-أب** — دخول ناعم بدل ما يفرقع في الوش

الباقي (انتقالات الشاشات) تحسينات صغيرة.

## «المنبه شغال 100%»

السيرفر فعلاً 100%. المشكلة إن **الرنين نفسه** مسؤولية نظام
التشغيل مش السيرفر — ومحتاج:
- حزمة إشعارات محلية
- إذن `SCHEDULE_EXACT_ALARM` على أندرويد 12+
- الكود الأصلي في `mobile-alarm/` (موجود ومش موصول)

ده **يوم شغل كامل**، مش تحسين.

---

# 📋 ترتيب أقترحه

| # | الشغل | ليه دلوقتي |
|---|---|---|
| 1 | المنطقة الزمنية | سطر واحد، بيصلّح المزامنة لكل مستخدم برة مصر |
| 2 | `/focus/active` | الجلسة مابتضيعش لما تقفل التطبيق |
| 3 | إشعارات النظام | المنبه يرن فعلاً |
| 4 | ترقيم الرسايل | 500 رسالة = شاشة متخنقة |
| 5 | سلّم خطوط موحّد | يخلي كل تحسين بعده أرخص |
| 6 | ٣ حركات مفتاحية | إحساس، مش زينة |
| 7 | تحسينات الشات | رد · تعديل · مؤشر كتابة |
| 8 | واجهة اللعبة | ممتع بس مش على المسار الأساسي |

---

# ⚠️ حدود الفحص

- **القاعدة محاكاة في الذاكرة** — القيود على مستوى Postgres
  (unique المركّب) محتاجة تشغيل حقيقي.
- **`generateTodayTasks` ماتنفّذتش كاملة** — بتقرا
  `journey.step.goal.userId` (تداخل مستويين) والمحاكاة بتدعم
  مستوى واحد. **فحصت منطقها بالكود** بدل ما أدّعي إني شغّلتها.
- **Gemini ماتندهش** (مفيش مفتاح) — مسار الرفض اتأكد.
- **الواجهة ماتكومبايلتش** — مفيش Flutter SDK هنا.
- **إنذار كاذب صحّحته:** فحصي قال «حزمة إشعارات موجودة» لأن
  `flutter_localizations` طابقت `flutter_local`. رجعت وأكدت
  يدوياً: **مفيش أي حزمة إشعارات**.
