# 📊 جدول تتبع بناء «بال» — الترابط الكامل (المرجع الحي)

> القاعدة الصارمة: **كل قسم = باك إند (endpoint + قاعدة) → اختبار حي → شاشة Flutter مربوطة → تحقق وصول للسيرفر والقاعدة.**
> ✅ = مكتمل ومتحقق حي · 🟡 = باك إند جاهز والفرونت لاحقاً · 🔴 = ناقص/محتاج حسم
> آخر تحديث: 13 أغسطس 2026

---

## القسم 1 — التعارف + تسمية الرفيق + جبل الأهداف

| البند | الحالة | التحقق |
|---|---|---|
| `PATCH /api/auth/companion` (تسمية الرفيق) | ✅ | 16/16 اختبار + وصل القاعدة (`companionName`) |
| `GET /api/auth/me` بيرجع الاسم | ✅ | متحقق |
| اسم الرفيق محقون في سياق الـ AI | ✅ | `aiContext.service.js` |
| `POST /api/goals/dream` (كويز الـ AI) | 🟡 | مبني — **محتاج مفتاح Gemini شغال** |
| `POST /api/goals/dream/:id/answers` (خطة الـ AI) | 🟡 | مبني — محتاج المفتاح |
| `POST /api/goals/dream/:id/approve` (تثبيت الجبل) | ✅ | 16/16 + القاعدة (Goal + GoalStep + أسبوع) |
| المسودات مخفية من القوائم | ✅ | متحقق |
| عزل المستخدمين | ✅ | متحقق |
| `GoalStep` (حقول الجبل) في المخطط | ✅ | `prisma db push` |

**المطلوب من المالك:** 🔑 **مفتاح Gemini جديد** (القديم 401 ميت) — من [aistudio.google.com/apikey](https://aistudio.google.com/apikey) → Regenerate → ضعه في `.env` (`GEMINI_API_KEY=...`).

---

## القسم 5 — المنبه (mobile-alarm الحقيقي — الوحيد في المشروع)

| البند | الحالة |
|---|---|
| محرك mobile-alarm النيتيف (Kotlin/Swift) | ✅ الوحيد — نسخة Flutter مطابقة 100% (diff صفر) |
| مسألة حسابية محلية أوفلاين | ✅ في `AlarmActivity.kt` (newChallenge) — تعمل بلا إنترنت |
| `POST /api/alarms/snooze` | ✅ نداء Gemini حقيقي (بلا فولباك وهمي — 502 صريح عند تعطل الـ AI + offlineFallback: MATH) |
| `POST /api/alarms/verify-wake` | ✅ اختيار عشوائي (مسألة/تصوير) + توقيع HMAC |
| `POST /api/alarms/wake-log` | ✅ تسجيل الاستيقاظ → WakeLog + شرارات + سلسلة |
| مسح الوهمي | ✅ حُذف: alarm_state_machine + alarm_solve_modal + audio_state_machine |
| شاشة إدارة منبهات Flutter | ✅ حقيقية: GET/POST/PATCH/DELETE + setNativeAlarm/removeNativeAlarm |
| تنظيف المسارات | ✅ إزالة /photo-proof المكرر — مسارات نظيفة |
| الاختبارات | ✅ alarm-flow 18/18 · friendship-flow 21/21 · dream-flow 17/17 · **link-test 9/9** |
| حذف React الزائد | ✅ AlarmModule.kt + AlarmPackage.kt اتشالوا (البناء الأندرويد بيفتح) |
| مزامنة عند فتح التطبيق | ✅ AlarmSyncService: GET منبهات ← مزامنة نيتيف ← تسجيل استيقاظ أوفلاين |
| الربط النهائي | ✅ App → Server → DB كامل (اختبار حي 9/9) |

---

## الأقسام التالية (بانتظار التكليف)

| القسم | الحالة |
|---|---|
| 2 — الرسائل + الصداقة (نظام انستقرام) | 🔴 لم يبدأ |
| 3 — العشائر (عامة/خاصة + التحديات) | 🔴 لم يبدأ |
| 4 — التركيز (فردي/جماعي + الراحة ≤10د) | 🔴 لم يبدأ |
| 5 — المنبه (غفوة AI + إثبات عشوائي) | 🔴 لم يبدأ |
| 6 — التوثيق الأسبوعي (6 أسئلة + قفل) | 🟡 موجود بنظام قديم — يحتاج تعديل للرؤية الجديدة |
| 7 — المهام (+ روتين + AI قبل/بعد) | 🟡 موجود جزئياً — يحتاج تعديل |
| 8 — الـ AI (شات Gemini) | 🟡 موجود (المسار `/api/ai/message`) — محتاج المفتاح |
| 9 — شاشات Flutter (الواجهة الجديدة "بال") | 🔴 لم تبدأ |

---

## الحدود الصارمة المسجلة (من رؤية المالك — تُطبق في كل قسم)

| القاعدة | القيمة |
|---|---|
| راحة جلسة التركيز | **≤ 10 دقائق** — ممنوع أكتر (سيرفر + تطبيق) |
| إنشاء عشيرة خاصة | 1 كحد أقصى |
| الانضمام لعشائر | 7 كحد أقصى (خاصة + عامة) |
| عشائر عامة (اهتمامات) | 2 كحد أقصى |
| أعضاء العشيرة الخاصة | 15 كحد أقصى |
| طلبات صداقة يومياً | 10 كحد أقصى |
| رسائل لغير الأصدقاء | رسالة واحدة = طلب صداقة (نظام انستقرام) |
| ممنوع الخروج من جلسة التركيز بعد الدخول | صارم |
| الخروج من التحدي | مسموح فقط قبل بدء الجلسة |
| أهداف الجبل | آخر خطوة = القمة = الهدف النهائي |


## القسم 7 — المهام (مكتمل — رؤية المالك)

| البند | الحالة |
|---|---|
| `Task.routineType` (DAILY = كل يوم) | ✅ في السكيما + db push |
| بلوكات بروتين يومي | ✅ `batch-blocks` يقبل routineType |
| إتمام روتين → نسخة الغد تلقائياً (بلا تكرار) | ✅ مُختبَر حي |
| المكتملة تختفي من القوائم | ✅ الافتراضي = معلّقة فقط · `filter=completed` للتاريخ |
| نكشة الـ AI قبل المهمة بـ 5 دقايق | ✅ طابور `task-nudge` + نداء Gemini حقيقي + إشعار في القاعدة |
| تذكير نظام صادق لو الـ AI معطّل | ✅ `source: SYSTEM` (بلا ادعاء AI) |
| قواعد عدم اللوم بعد المهمة | ✅ موجودة في aiRules (احتواء قبل الحل) |
| الاختبارات | ✅ task 30/30 · task-routine-flow 16/16 |

## 📊 الفحص الشامل النهائي — 13 أغسطس 2026

**35 حزمة اختبار · 762 اختبار أخضر · صفر فشل**
(كل حزمة شغّلت حية ضد القاعدة الحقيقية أو الموك المعكوس للسكيما)

**إصلاحات ضمن الفحص (صفر وهمي):**
- mock-prisma عكس السكيما: notIn · participants.some · select.steps · draft · showcaseIds · lastMessageAt · cascade المحادثة · aiPulseEvent unique · aiConversation.messages · syncedAt
- اختبارات chat + integration + muscular حدّثت لرؤية الصداقة (نظام انستقرام)
- analytics.service: حماية members (كان يقع 500 لو relation ناقصة)
- listConversations: غير المقروء = استعلام واحد (لا N+1)
- ai.test: عدد اللحظات 10 (TASK_FOLLOWUP + TASK_PRE_REMINDER)
- chat.controller: الحد اليومي 429 + دمج طلبات الصداقة في تبويب الطلبات
- e2e.test.mjs القديم الميت (leaderboard محذوف) حُذف


## الجولة الأخيرة — كل الناقص اتنفذ (13 أغسطس 2026)

### القسم 4 — التركيز (الدورة المخصصة) ✅
- `focusMin + restMin + cycles` في startSession — (تركيز + راحة) × تكرار، وآخر دورة = لوبي
- 🔴 قيد صارم: راحة 1-10 فقط (السيرفر يرفض الأكثر — 400)
- الشرارات من `totalFocusMin` (التركيز الفعلي فقط — الراحة لا تُحتسب)
- `getActiveSession` يرجع الطور الحالي (FOCUS/REST) + رقم الدورة
- الاختبارات: focus-cycle 20/20 · focus القديم 26/26

### القسم 3 — العشائر (الحدود + التحدي الجماعي) ✅
- حد 15 عضو للعشيرة الخاصة (كان 50)
- حد 7 عشائر انضمام (خاصة + عامة) — MAX_CLANS_REACHED
- التحدي الجماعي الكامل:
  - الأدمن ينشئ تحدي → إشعار FOCUS_CHALLENGE لكل الأعضاء
  - قبول/تأجيل/مغادرة (قبل البدء فقط)
  - الانطلاق → جلسة CHALLENGE لكل مشارك بالدورة المخصصة (restMin ≤10 صارم)
  - ممنوع الخروج بعد البدء (التركيز مقدس) — 409
- الاختبارات: challenge-flow 17/17

### القسم 6 — التوثيق الأسبوعي ✅
- `GoalWeek.answers Json?` — الأسئلة الستة + الأسئلة المخصصة (زر +)
- نافذة التعديل 10 دقائق (بدل 5) — الرسالة محدثة
- الاختبارات: journal 51/51

### لعبة الثعبان الجماعية — الربط الحقيقي ✅
- `SnakeMultiplayerWidget` جديد — يتصل بمحرك السيرفر (/game) عبر Socket.io:
  - join_room · change_direction (D-Pad) · boost
  - يستقبل player_joined (roster) · game_state_update (30fps) · game_over
- يستبدل اللعبة المحلية البسيطة في BreakHub
- ApiClient.getAccessToken + socketHost للويب (أصل الصفحة)
- flutter analyze: صفر أخطاء

### القسم 7 — نظام الجبل الكامل: الحلم → أهداف → رحلات → أيام → مهام → قمة 🏔️ (2026-08-13) ✅
- **الجداول الجديدة**: `Journey` (goalStepId unique · status DRAFT/ACTIVE/COMPLETED · currentDay · approvedAt · completedAt) + `JourneyDay` (journeyId · dayNumber · title · description · scheduledDate · status PENDING/COMPLETED · @@unique([journeyId, dayNumber]))
- **تعديل Task**: + source (MANUAL/JOURNEY) + journeyDayId (unique — ضد التكرار) + goalStepId
- **تعديل NotificationType**: + TASK_CHECKIN
- **خدمات جديدة**:
  - `journeyPlanner.service.js` — AI حقيقي يبني `{days:[...]}` لهدف واحد (مرة واحدة بس)
  - `journeyScheduler.service.js` — صفر AI: يحوّل JourneyDay[currentDay] → Task في قسم المهام (Idempotent)
  - `taskCheckIn.service.js` — اطمئنان بعد 10 دقايق من الإنجاز (persona TASK_FOLLOWUP) → Notification
- **مسارات جديدة**: POST /steps/:stepId/journey (توليد DRAFT) · POST /steps/:stepId/journey/approve (ACTIVE + توزيع تواريخ + توليد مهمة اليوم 1) · GET /steps/:stepId/journey (عرض + progress + متأخر N)
- **سلسلة التقدم** (في completeTask — transaction واحدة): Task ✅ → JourneyDay ✅ → كل الأيام → Journey COMPLETED + GoalStep ✅ → كل المراحل → Goal.completedAt 🏁
- **Queues**: JOURNEY_DAILY (شبكة أمان كل ساعتين) + TASK_CHECKIN (مؤجل 10 دقايق) + Lazy عند الـ login
- **المرونة**: اليوم الجاي مفتوح دايماً · لا تكرار مهام · مستخدم تاني 404
- **قرارات المالك الثابتة**: لا كويز إطلاقاً · صفر وهمي · الاختبار الحي من ملفات test/
- **الاختبارات**: mountain-lifecycle 25/25 · dream-flow 17/17 · task-routine-flow 16/16 · alarm-flow 18/18 · focus-cycle 20/20 · challenge-flow 17/17 · friendship-flow 21/21

### القسم 8 — مزامنة منتصف الليل المحلي (2026-08-13) ✅
- **القاعدة (قرار المالك)**: الساعة 12 منتصف الليل عند العميل (بتوقيت منطقته الزمنية) → يتم توليد/مزامنة مهام اليوم تلقائياً.
- **التنفيذ**:
  - `journeyScheduler.service.js`: بوابة `scheduledDate <= اليوم المحلي للمستخدم` (عبر `User.timezone` + `Intl` — `localDate` من streak.service) → اليوم المستقبلي `deferred` ولا يتولد قبل معاده · idempotent زي ما هو · + إشعار "مهامك لليوم جاهزة 🏔️" عند التوليد.
  - `journal.controller.js` (approveStepJourney): توزيع تواريخ الأيام صار **باليوم المحلي للمستخدم** (كان بوقت السيرفر = خطأ).
  - `queues/index.js`: الجوب المتكرر من كل ساعتين → **كل 15 دقيقة** (يلتقط منتصف ليل كل مستخدم بأقصى فرق 15 دقيقة) + الـ lazy عند فتح التطبيق كطبقة فورية.
- **الإثبات الحي**: `mountain-lifecycle` **29/29** — قسم جديد: مستخدمان UTC+14 / UTC-11 → كل واحد أخد "نهارده" المحلي + يوم "بكرة" المحلي لسه deferred · الاختبارات القديمة: dream-flow 17/17 · task-routine 16/16 · focus-cycle 20/20.
