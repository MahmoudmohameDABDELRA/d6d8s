# 🏔️ بال — دليل التشغيل

> فك ضغط `bal-project.zip` وابدأ من هنا.

---

## ١) التطبيق (ابدأ بده)

```bash
cd bal-app-code/clan-app/bal_app

flutter pub get
flutter analyze      # ⚠️ أهم خطوة — اقرا التحذير تحت
flutter run
```

### ⚠️ عن `flutter analyze`

الكود اتكتب بعناية بس **متكومبايلش** — مفيش Flutter SDK في بيئة التطوير
اللي اشتغلت فيها. الأمر ده هيكشف أي غلطة كتابة.

**لو طلّع أي أخطاء، انسخها وابعتهالي.**

### على موبايل حقيقي

التطبيق افتراضياً بيدوّر على السيرفر عند `10.0.2.2:3000` (ده عنوان
محاكي أندرويد). على موبايل حقيقي لازم IP جهازك:

```bash
flutter run --dart-define=API_BASE_URL=http://192.168.1.X:3000
```

(اعرف الـ IP بـ `ipconfig` على ويندوز أو `ifconfig` على ماك/لينكس)

---

## ٢) السيرفر

من غيره التطبيق هيفتح والبوب-أب هيطلع في ميعاده، **بس لما تكتب رد
مش هيلاقي حد يرد عليك**.

```bash
cd bal-app-code/clan-app

cp .env.example .env      # ثم عدّل القيم — شوف تحت
npm install
docker compose up -d      # بوستجرس + ريديس
npm run prisma:generate
npm run prisma:deploy

npm run dev               # السيرفر
npm run worker            # في تيرمينال تاني
```

### اللي لازم تغيّره في `.env`

| المفتاح | تعمل إيه |
|---|---|
| `JWT_ACCESS_SECRET` | `openssl rand -base64 48` |
| `JWT_REFRESH_SECRET` | نفس الأمر، **قيمة مختلفة** |
| `GEMINI_API_KEY` | المفتاح الجديد من Google AI Studio |

⚠️ **`.env` مش موجود في الزيب عن قصد** — ملفات الأسرار ما تتشاركش.

---

## ٣) اتأكد إن كل حاجة سليمة

```bash
cd bal-app-code/clan-app
npm run test:checkin   # حلقة الاطمئنان
npm run test:dream     # مسار الحلم
npm run test:app       # فحص كود Flutter + التكبير
```

مش محتاجين قاعدة بيانات ولا نت ولا مفتاح Gemini.

---

## 🔴 حاجة واحدة محتاجة إيدك

**مفتاح `GEMINI_API_KEY` القديم متسرّب.**

كان جوه ملف مضغوط اترفع في أول commit على جيتهب، فبقى في تاريخ الريبو
للأبد. أي حد يشوف الريبو يقدر يوصله.

**اعمل إيه:** ادخل [Google AI Studio](https://aistudio.google.com/apikey)
← امسح المفتاح القديم ← ولّد واحد جديد ← حطه في `.env`.

المفتاح ده بيتحاسب عليك، فأي حد معاه يقدر يصرف من رصيدك.

---

## 🗺️ فين الحاجات

```
bal-app-code/clan-app/
├── bal_app/          📱 تطبيق Flutter — ده اللي هتشغله
│   └── lib/
│       ├── core/checkin/     ⏰ مراقب مواعيد المهام
│       ├── widgets/          🎨 البوب-أب والأزرار والكروت
│       └── screens/          الجبل · المهام · الشات · البروفايل
│
├── src/              ⚙️ السيرفر (Node.js)
│   ├── modules/      نقاط الـ API
│   ├── services/     المنطق + طبقة الـ AI
│   └── queues/       الجوبس (المزامنة اليومية والاطمئنان)
│
├── prisma/           🗄️ قاعدة البيانات
└── test/             🧪 الاختبارات
```

**ملفات الشرح في جذر المشروع:**

| الملف | فيه إيه |
|---|---|
| `CHECKIN-FLOW.md` | حلقة الاطمئنان كاملة + عقد الـ API |
| `PROJECT-REVIEW.md` | مراجعة المشروع كله |
| `STRUCTURE.md` | شجرة الملفات |
| `README.md` | نظرة عامة |

---

## ✅ اللي اتعمل في الشغل ده

**١. حلقة الاطمئنان** — البوب-أب بيطلع لوحده لما وقت المهمة يخلص،
تكتب اللي حصل، ورد رفيقك يظهر في نفس المكان. مع 24 صيغة سؤال عشان
ما تحسّش إن السؤال ثابت.

**٢. مسار الحلم** — بياناتك من الأونبوردنج بقت توصل للـ AI، والموافقة
على الجبل بتولّد أول رحلة تلقائياً.

**٣. تكبير الواجهة 15%** — الأزرار والكروت والخطوط.

**٤. ٥ باجات اتصلحوا** لقيتهم وأنا بفحص — أخطرهم إن قاعدة «ممنوع لوم
المستخدم» مكانتش بتتطبق خالص.

**٥. ٤٦ اختبار + ١٩١ فحص** — كلهم بينجحوا.

---

## 📍 نفس الشغل على جيتهب

```
https://github.com/MahmoudmohameDABDELRA/d6d8s
الفرع: arena/01a000e4-d6d8s     ← مش main
```
